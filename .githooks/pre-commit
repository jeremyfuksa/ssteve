#!/usr/bin/env bash
set -euo pipefail

# The old branch-family checks (gemini/*, assistant/*) guarded directories from
# a previous repo layout (sstv-station/, core/python/) that no longer exists.
# They were removed in the 2026-07 CLAUDE.md audit cleanup (issue #11).
# This hook is currently a no-op.

exit 0
